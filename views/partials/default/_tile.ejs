<%
	var hasPath = item.path;
	var image = item.thumbnail ? item.thumbnail : item.img ? item.img : (item.featured && item.featured.thumbnail ? item.featured.thumbnail : '');
%>
<div class="item default">

    <h3 id="<%= item.key || key %>"><%- link_to(item.path, item.title) %></h3>
    <% if (image) { %>
    <%- link_to(item.path, '<img class="preview" src="'+image+'" />') %>
    <% } %>
    <div>
        <p class="introText"><%- item.previewText ? item.previewText : item.introText %></p>
    </div>
    <% // todo linkto with classes and skip if no link 
    if (hasPath) { %><a class="link action" href="<%= item.path %>"><%- (item.actionText ? item.actionText : 'Learn more')+' &raquo;' %></a><% } %>
</div>

