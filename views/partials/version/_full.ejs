<div class="featured" class="mainElement">
    <div class="slide version">
        <div class="caption">
            <h3><%= item.title %> <%= item.version %></h3>
            <%- item.introText||item.summary %>
            <% include _download %> 
            <br/>
            <p>Download and unzip. <br/>From the folder start Neo4j Server <br/>on Linux/Mac with <code>bin/neo4j start</code> from a terminal.<br/> On Win use <code>bin\Neo4j.bat start</code><br/> or double-click.<br/>
               Open <a href="http://localhost:7474" target="_blank"><code>http://localhost:7474</code></a> for the Web UI.</p>
            <a class="link" href="<%= item.path||item.src %>" target="_blank">Read More</a>
        </div>
        <div class="content">
            <% if (item.thumbnail) { %>
                <img class="thumbnail" src="<%= item.thumbnail %>" data-src="<%= item.src %>">
            <% } %>
        </div>
        <div class="clear"></div>
    </div>
</div><!-- featured -->
