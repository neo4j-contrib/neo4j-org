<div class="tn track">
	<% var hasPath = related.path %>

    <!--h4><% if (hasPath) { %><a href="<%= related.path %>"><% } %><%= related.title %><% if (hasPath) { %></a><% } %></h4-->
    <% if (related.img) { %>
    <a href="<%= related.path %>"><img class="logo" src="<%= related.img %>"></a>
    <% } %>
    <% if (hasPath) { %><a class="btn" href="<%= related.path %>"><%= (related.actionText ? related.actionText : 'Learn more')+' &raquo;' %></a><% } %>

</div>

